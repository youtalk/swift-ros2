// Generated by swift-ros2-gen — DO NOT EDIT.
// Source: sensor_msgs/msg/BatteryState

#include "Generated/crcl_marshal_battery_state.h"

#include <sensor_msgs/msg/battery_state.h>

#include "crcl_internal.h"
#include <rosidl_runtime_c/string_functions.h>
#include <rosidl_runtime_c/primitives_sequence_functions.h>
#include <rmw/rmw.h>
#include <rmw/serialized_message.h>
#include <rcl/publisher.h>
#include <rcutils/allocator.h>
#include <rosidl_runtime_c/message_type_support_struct.h>
#include <string.h>
#include <stdlib.h>
#include <stdbool.h>

const rosidl_message_type_support_t *crcl_typesupport_battery_state(void) {
    return ROSIDL_GET_MSG_TYPE_SUPPORT(sensor_msgs, msg, BatteryState);
}

static int fill_battery_state(sensor_msgs__msg__BatteryState *msg,
    int32_t header_stamp_sec,
    uint32_t header_stamp_nanosec,
    const char *header_frame_id,
    float voltage,
    float temperature,
    float current,
    float charge,
    float capacity,
    float design_capacity,
    float percentage,
    uint8_t power_supply_status,
    uint8_t power_supply_health,
    uint8_t power_supply_technology,
    bool present,
    const float *cell_voltage_data, size_t cell_voltage_count,
    const float *cell_temperature_data, size_t cell_temperature_count,
    const char *location,
    const char *serial_number) {
    memset(msg, 0, sizeof(*msg));
    msg->header.stamp.sec = header_stamp_sec;
    msg->header.stamp.nanosec = header_stamp_nanosec;
    if (!rosidl_runtime_c__String__assign(&msg->header.frame_id, header_frame_id ? header_frame_id : "")) {
        crcl__set_error("String__assign failed for header_frame_id");
        return 0;
    }
    msg->voltage = voltage;
    msg->temperature = temperature;
    msg->current = current;
    msg->charge = charge;
    msg->capacity = capacity;
    msg->design_capacity = design_capacity;
    msg->percentage = percentage;
    msg->power_supply_status = power_supply_status;
    msg->power_supply_health = power_supply_health;
    msg->power_supply_technology = power_supply_technology;
    msg->present = present;
    if (!rosidl_runtime_c__float__Sequence__init(&msg->cell_voltage, cell_voltage_count)) {
        crcl__set_error("cell_voltage sequence init failed");
        return 0;
    }
    if (cell_voltage_count) {
        memcpy(msg->cell_voltage.data, cell_voltage_data, cell_voltage_count * sizeof(float));
    }
    if (!rosidl_runtime_c__float__Sequence__init(&msg->cell_temperature, cell_temperature_count)) {
        crcl__set_error("cell_temperature sequence init failed");
        return 0;
    }
    if (cell_temperature_count) {
        memcpy(msg->cell_temperature.data, cell_temperature_data, cell_temperature_count * sizeof(float));
    }
    if (!rosidl_runtime_c__String__assign(&msg->location, location ? location : "")) {
        crcl__set_error("String__assign failed for location");
        return 0;
    }
    if (!rosidl_runtime_c__String__assign(&msg->serial_number, serial_number ? serial_number : "")) {
        crcl__set_error("String__assign failed for serial_number");
        return 0;
    }
    return 1;
}

static void fini_battery_state(sensor_msgs__msg__BatteryState *msg) {
    sensor_msgs__msg__BatteryState__fini(msg);
}

int crcl_serialize_battery_state(
    int32_t header_stamp_sec,
    uint32_t header_stamp_nanosec,
    const char *header_frame_id,
    float voltage,
    float temperature,
    float current,
    float charge,
    float capacity,
    float design_capacity,
    float percentage,
    uint8_t power_supply_status,
    uint8_t power_supply_health,
    uint8_t power_supply_technology,
    bool present,
    const float *cell_voltage_data, size_t cell_voltage_count,
    const float *cell_temperature_data, size_t cell_temperature_count,
    const char *location,
    const char *serial_number,
    uint8_t **out_buf, size_t *out_len) {
    const rosidl_message_type_support_t *ts = crcl_typesupport_battery_state();
    sensor_msgs__msg__BatteryState msg;
    if (!fill_battery_state(&msg, header_stamp_sec, header_stamp_nanosec, header_frame_id, voltage, temperature, current, charge, capacity, design_capacity, percentage, power_supply_status, power_supply_health, power_supply_technology, present, cell_voltage_data, cell_voltage_count, cell_temperature_data, cell_temperature_count, location, serial_number)) {
        fini_battery_state(&msg);
        return -1;  // crcl__set_error already called
    }
    rcutils_allocator_t alloc = rcutils_get_default_allocator();
    rmw_serialized_message_t ser = rmw_get_zero_initialized_serialized_message();
    rmw_ret_t rc = rmw_serialized_message_init(&ser, 0u, &alloc);
    if (rc != RMW_RET_OK) {
        crcl__capture_rcl_error();
        fini_battery_state(&msg);
        return (int)rc;
    }
    rc = rmw_serialize(&msg, ts, &ser);
    if (rc != RMW_RET_OK) {
        crcl__capture_rcl_error();
        (void)rmw_serialized_message_fini(&ser);
        fini_battery_state(&msg);
        return (int)rc;
    }
    uint8_t *copy = malloc(ser.buffer_length);
    if (!copy) {
        crcl__set_error("out-of-memory copying serialized message");
        (void)rmw_serialized_message_fini(&ser);
        fini_battery_state(&msg);
        return -1;
    }
    memcpy(copy, ser.buffer, ser.buffer_length);
    *out_buf = copy;
    *out_len = ser.buffer_length;
    (void)rmw_serialized_message_fini(&ser);
    fini_battery_state(&msg);
    return 0;
}

int crcl_publish_battery_state(
    crcl_publisher_t *pub,
    int32_t header_stamp_sec,
    uint32_t header_stamp_nanosec,
    const char *header_frame_id,
    float voltage,
    float temperature,
    float current,
    float charge,
    float capacity,
    float design_capacity,
    float percentage,
    uint8_t power_supply_status,
    uint8_t power_supply_health,
    uint8_t power_supply_technology,
    bool present,
    const float *cell_voltage_data, size_t cell_voltage_count,
    const float *cell_temperature_data, size_t cell_temperature_count,
    const char *location,
    const char *serial_number) {
    if (!pub) {
        crcl__set_error("crcl_publish_battery_state: null publisher");
        return -1;
    }
    sensor_msgs__msg__BatteryState msg;
    if (!fill_battery_state(&msg, header_stamp_sec, header_stamp_nanosec, header_frame_id, voltage, temperature, current, charge, capacity, design_capacity, percentage, power_supply_status, power_supply_health, power_supply_technology, present, cell_voltage_data, cell_voltage_count, cell_temperature_data, cell_temperature_count, location, serial_number)) {
        fini_battery_state(&msg);
        return -1;  // crcl__set_error already called
    }
    rcl_ret_t ret = rcl_publish(&pub->pub, &msg, NULL);
    fini_battery_state(&msg);
    if (ret != RCL_RET_OK) {
        crcl__capture_rcl_error();
        return (int)ret;
    }
    return 0;
}
