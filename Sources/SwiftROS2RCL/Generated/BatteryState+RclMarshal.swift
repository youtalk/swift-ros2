// Generated by swift-ros2-gen — DO NOT EDIT.
// Source: sensor_msgs/msg/BatteryState

import CRclBridge
import Foundation
import SwiftROS2Messages
import SwiftROS2Transport

/// Serialize `value` via the real ROS 2 introspection serializer
/// (rmw_serialize), returning the on-wire CDR bytes (incl. the 4-byte
/// encapsulation header).
package func rclSerializeBatteryState(_ value: BatteryState) throws -> [UInt8] {
    var outBuf: UnsafeMutablePointer<UInt8>?
    var outLen: Int = 0
    let rc: Int32 =
        value.cellVoltage.withUnsafeBufferPointer { cellVoltageBuf in
            value.cellTemperature.withUnsafeBufferPointer { cellTemperatureBuf in
                crcl_serialize_battery_state(
                    value.header.stamp.sec,
                    value.header.stamp.nanosec,
                    value.header.frameId,
                    value.voltage,
                    value.temperature,
                    value.current,
                    value.charge,
                    value.capacity,
                    value.designCapacity,
                    value.percentage,
                    value.powerSupplyStatus,
                    value.powerSupplyHealth,
                    value.powerSupplyTechnology,
                    value.present,
                    cellVoltageBuf.baseAddress,
                    cellVoltageBuf.count,
                    cellTemperatureBuf.baseAddress,
                    cellTemperatureBuf.count,
                    value.location,
                    value.serialNumber, &outBuf, &outLen)
            }
        }
    guard rc == 0, let outBuf else {
        throw TransportError.publishFailed(String(cString: crcl_last_error()))
    }
    defer { crcl_free(outBuf) }
    return Array(UnsafeBufferPointer(start: outBuf, count: outLen))
}

extension BatteryState: RclTypedPublishable {
    /// Marshal this BatteryState into its rosidl C struct and publish
    /// via rcl_publish (crcl_publish_battery_state).
    package func rclTypedPublish(into handle: any RclPublisherHandle) throws {
        guard let box = handle as? RclPublisherBox else {
            throw TransportError.publishFailed(
                "rclTypedPublish: unexpected publisher handle type")
        }
        let rc: Int32? =
            cellVoltage.withUnsafeBufferPointer { cellVoltageBuf in
                cellTemperature.withUnsafeBufferPointer { cellTemperatureBuf in
                    box.withPtr { p in
                        crcl_publish_battery_state(
                            p,
                            header.stamp.sec,
                            header.stamp.nanosec,
                            header.frameId,
                            voltage,
                            temperature,
                            current,
                            charge,
                            capacity,
                            designCapacity,
                            percentage,
                            powerSupplyStatus,
                            powerSupplyHealth,
                            powerSupplyTechnology,
                            present,
                            cellVoltageBuf.baseAddress,
                            cellVoltageBuf.count,
                            cellTemperatureBuf.baseAddress,
                            cellTemperatureBuf.count,
                            location,
                            serialNumber)
                    }
                }
            }
        guard let rc else { throw TransportError.publisherClosed }
        if rc != 0 { throw TransportError.publishFailed(String(cString: crcl_last_error())) }
    }
}
