// Generated by swift-ros2-gen — DO NOT EDIT.
// Source: sensor_msgs/msg/CameraInfo

import CRclBridge
import Foundation
import SwiftROS2Messages
import SwiftROS2Transport

/// Serialize `value` via the real ROS 2 introspection serializer
/// (rmw_serialize), returning the on-wire CDR bytes (incl. the 4-byte
/// encapsulation header).
package func rclSerializeCameraInfo(_ value: CameraInfo) throws -> [UInt8] {
    var outBuf: UnsafeMutablePointer<UInt8>?
    var outLen: Int = 0
    precondition(value.k.count == 9, "k needs 9 elements")
    precondition(value.r.count == 9, "r needs 9 elements")
    precondition(value.p.count == 12, "p needs 12 elements")
    let rc: Int32 =
        value.d.withUnsafeBufferPointer { dBuf in
            value.k.withUnsafeBufferPointer { kBuf in
                value.r.withUnsafeBufferPointer { rBuf in
                    value.p.withUnsafeBufferPointer { pBuf in
                        crcl_serialize_camera_info(
                            value.header.stamp.sec,
                            value.header.stamp.nanosec,
                            value.header.frameId,
                            value.height,
                            value.width,
                            value.distortionModel,
                            dBuf.baseAddress,
                            dBuf.count,
                            kBuf.baseAddress,
                            rBuf.baseAddress,
                            pBuf.baseAddress,
                            value.binningX,
                            value.binningY,
                            value.roi.xOffset,
                            value.roi.yOffset,
                            value.roi.height,
                            value.roi.width,
                            value.roi.doRectify, &outBuf, &outLen)
                    }
                }
            }
        }
    guard rc == 0, let outBuf else {
        throw TransportError.publishFailed(String(cString: crcl_last_error()))
    }
    defer { crcl_free(outBuf) }
    return Array(UnsafeBufferPointer(start: outBuf, count: outLen))
}

extension CameraInfo: RclTypedPublishable {
    /// Marshal this CameraInfo into its rosidl C struct and publish
    /// via rcl_publish (crcl_publish_camera_info).
    package func rclTypedPublish(into handle: any RclPublisherHandle) throws {
        guard let box = handle as? RclPublisherBox else {
            throw TransportError.publishFailed(
                "rclTypedPublish: unexpected publisher handle type")
        }
        precondition(k.count == 9, "k needs 9 elements")
        precondition(r.count == 9, "r needs 9 elements")
        precondition(p.count == 12, "p needs 12 elements")
        let rc: Int32? =
            d.withUnsafeBufferPointer { dBuf in
                k.withUnsafeBufferPointer { kBuf in
                    r.withUnsafeBufferPointer { rBuf in
                        p.withUnsafeBufferPointer { pBuf in
                            box.withPtr { p in
                                crcl_publish_camera_info(
                                    p,
                                    header.stamp.sec,
                                    header.stamp.nanosec,
                                    header.frameId,
                                    height,
                                    width,
                                    distortionModel,
                                    dBuf.baseAddress,
                                    dBuf.count,
                                    kBuf.baseAddress,
                                    rBuf.baseAddress,
                                    pBuf.baseAddress,
                                    binningX,
                                    binningY,
                                    roi.xOffset,
                                    roi.yOffset,
                                    roi.height,
                                    roi.width,
                                    roi.doRectify)
                            }
                        }
                    }
                }
            }
        guard let rc else { throw TransportError.publisherClosed }
        if rc != 0 { throw TransportError.publishFailed(String(cString: crcl_last_error())) }
    }
}
