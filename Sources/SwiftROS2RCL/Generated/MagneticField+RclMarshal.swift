// Generated by swift-ros2-gen — DO NOT EDIT.
// Source: sensor_msgs/msg/MagneticField

import CRclBridge
import Foundation
import SwiftROS2Messages
import SwiftROS2Transport

/// Serialize `value` via the real ROS 2 introspection serializer
/// (rmw_serialize), returning the on-wire CDR bytes (incl. the 4-byte
/// encapsulation header).
package func rclSerializeMagneticField(_ value: MagneticField) throws -> [UInt8] {
    var outBuf: UnsafeMutablePointer<UInt8>?
    var outLen: Int = 0
    precondition(value.magneticFieldCovariance.count == 9, "magnetic_field_covariance needs 9 elements")
    let rc: Int32 =
        value.magneticFieldCovariance.withUnsafeBufferPointer { magneticFieldCovarianceBuf in
            crcl_serialize_magnetic_field(
                value.header.stamp.sec,
                value.header.stamp.nanosec,
                value.header.frameId,
                value.magneticField.x,
                value.magneticField.y,
                value.magneticField.z,
                magneticFieldCovarianceBuf.baseAddress, &outBuf, &outLen)
        }
    guard rc == 0, let outBuf else {
        throw TransportError.publishFailed(String(cString: crcl_last_error()))
    }
    defer { crcl_free(outBuf) }
    return Array(UnsafeBufferPointer(start: outBuf, count: outLen))
}

extension MagneticField: RclTypedPublishable {
    /// Marshal this MagneticField into its rosidl C struct and publish
    /// via rcl_publish (crcl_publish_magnetic_field).
    package func rclTypedPublish(into handle: any RclPublisherHandle) throws {
        guard let box = handle as? RclPublisherBox else {
            throw TransportError.publishFailed(
                "rclTypedPublish: unexpected publisher handle type")
        }
        precondition(magneticFieldCovariance.count == 9, "magnetic_field_covariance needs 9 elements")
        let rc: Int32? =
            magneticFieldCovariance.withUnsafeBufferPointer { magneticFieldCovarianceBuf in
                box.withPtr { p in
                    crcl_publish_magnetic_field(
                        p,
                        header.stamp.sec,
                        header.stamp.nanosec,
                        header.frameId,
                        magneticField.x,
                        magneticField.y,
                        magneticField.z,
                        magneticFieldCovarianceBuf.baseAddress)
                }
            }
        guard let rc else { throw TransportError.publisherClosed }
        if rc != 0 { throw TransportError.publishFailed(String(cString: crcl_last_error())) }
    }
}
