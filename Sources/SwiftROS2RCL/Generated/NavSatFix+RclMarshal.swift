// Generated by swift-ros2-gen — DO NOT EDIT.
// Source: sensor_msgs/msg/NavSatFix

import CRclBridge
import Foundation
import SwiftROS2Messages
import SwiftROS2Transport

/// Serialize `value` via the real ROS 2 introspection serializer
/// (rmw_serialize), returning the on-wire CDR bytes (incl. the 4-byte
/// encapsulation header).
package func rclSerializeNavSatFix(_ value: NavSatFix) throws -> [UInt8] {
    var outBuf: UnsafeMutablePointer<UInt8>?
    var outLen: Int = 0
    precondition(value.positionCovariance.count == 9, "position_covariance needs 9 elements")
    let rc: Int32 =
        value.positionCovariance.withUnsafeBufferPointer { positionCovarianceBuf in
            crcl_serialize_nav_sat_fix(
                value.header.stamp.sec,
                value.header.stamp.nanosec,
                value.header.frameId,
                value.status.status,
                value.status.service,
                value.latitude,
                value.longitude,
                value.altitude,
                positionCovarianceBuf.baseAddress,
                value.positionCovarianceType, &outBuf, &outLen)
        }
    guard rc == 0, let outBuf else {
        throw TransportError.publishFailed(String(cString: crcl_last_error()))
    }
    defer { crcl_free(outBuf) }
    return Array(UnsafeBufferPointer(start: outBuf, count: outLen))
}

extension NavSatFix: RclTypedPublishable {
    /// Marshal this NavSatFix into its rosidl C struct and publish
    /// via rcl_publish (crcl_publish_nav_sat_fix).
    package func rclTypedPublish(into handle: any RclPublisherHandle) throws {
        guard let box = handle as? RclPublisherBox else {
            throw TransportError.publishFailed(
                "rclTypedPublish: unexpected publisher handle type")
        }
        precondition(positionCovariance.count == 9, "position_covariance needs 9 elements")
        let rc: Int32? =
            positionCovariance.withUnsafeBufferPointer { positionCovarianceBuf in
                box.withPtr { p in
                    crcl_publish_nav_sat_fix(
                        p,
                        header.stamp.sec,
                        header.stamp.nanosec,
                        header.frameId,
                        status.status,
                        status.service,
                        latitude,
                        longitude,
                        altitude,
                        positionCovarianceBuf.baseAddress,
                        positionCovarianceType)
                }
            }
        guard let rc else { throw TransportError.publisherClosed }
        if rc != 0 { throw TransportError.publishFailed(String(cString: crcl_last_error())) }
    }
}
